import type { ReactElement } from 'react';

// brand: powered-by-lenken på login-siden er fjernet i denne forken.
export const LoginPoweredBy = (): ReactElement | null => null;

export default LoginPoweredBy;
